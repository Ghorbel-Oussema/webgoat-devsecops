name: DevSecOps Scan

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read
  security-events: write
  actions: read
  packages: write

# OPTIONAL SECRETS:
#   SEMGREP_APP_TOKEN
#   SONAR_HOST_URL
#   SONAR_TOKEN

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ---------- Vars ----------
      - name: Compute image name (lowercase)
        run: |
          echo "IMAGE=ghcr.io/${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV
          echo "TAG_SHA=${GITHUB_SHA}" >> $GITHUB_ENV

      # ---------- Toolchains ----------
      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Detect Node (package.json)
        id: detect_node
        run: |
          if [ -f package.json ]; then
            echo "has_node=true" >> $GITHUB_OUTPUT
          else
            echo "has_node=false" >> $GITHUB_OUTPUT
          fi

      # ---------- Prepare folders ----------
      - name: Prepare folders
        run: |
          mkdir -p reports/meta
          mkdir -p reports/sast/semgrep reports/sast/bearer reports/sast/node reports/sast/sonar
          mkdir -p reports/dast/zap reports/dast/nuclei reports/dast/wapiti
          mkdir -p reports/ai ai

      # =========================
      # 1) Build JAR
      # =========================
      - name: Build jar (skip tests)
        run: |
          set -eux
          mvn --no-transfer-progress -DskipTests clean package
          ls -la target || true

      # =========================
      # 2) Build & Push Docker image to GHCR
      # =========================
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push image (latest + sha)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ env.TAG_SHA }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # =========================
      # 3) SAST - Semgrep (JSON + SARIF)
      # =========================
      - name: Install Semgrep
        run: |
          python -m pip install --upgrade pip
          pip install semgrep

      - name: Semgrep scan (JSON + SARIF)
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}
        run: |
          set -eux
          semgrep --config p/security-audit --sarif -o reports/sast/semgrep/semgrep.sarif . || true
          semgrep --config p/security-audit --json  -o reports/sast/semgrep/semgrep.json  . || true
          test -f reports/sast/semgrep/semgrep.json || echo '{}' > reports/sast/semgrep/semgrep.json

      - name: Upload Semgrep SARIF to GitHub Security
        if: always() && hashFiles('reports/sast/semgrep/semgrep.sarif') != ''
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: reports/sast/semgrep/semgrep.sarif

      # =========================
      # 4) SAST - CodeQL (Java)
      # =========================
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: java
          queries: security-extended

      - name: Autobuild (CodeQL)
        uses: github/codeql-action/autobuild@v4

      - name: Analyze (CodeQL)
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:java"

      # =========================
      # 5) SAST - Bearer (JSON + SARIF)
      # =========================
      - name: Bearer scan (JSON + SARIF) - fixed
        run: |
          set -eux
          mkdir -p reports/sast/bearer

          docker run --rm -v "$PWD:/repo" bearer/bearer:latest \
            scan /repo --format json > reports/sast/bearer/bearer.json || true

          docker run --rm -v "$PWD:/repo" bearer/bearer:latest \
            scan /repo --format sarif > reports/sast/bearer/bearer.sarif || true

          test -f reports/sast/bearer/bearer.json || echo '{}' > reports/sast/bearer/bearer.json

          if [ ! -s reports/sast/bearer/bearer.sarif ]; then
            rm -f reports/sast/bearer/bearer.sarif
          else
            if ! grep -q '"version"' reports/sast/bearer/bearer.sarif || ! grep -q '"runs"' reports/sast/bearer/bearer.sarif; then
              echo "Bearer SARIF not valid -> skipping upload"
              rm -f reports/sast/bearer/bearer.sarif
            fi
          fi

      - name: Upload Bearer SARIF to GitHub Security
        if: always() && hashFiles('reports/sast/bearer/bearer.sarif') != ''
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: reports/sast/bearer/bearer.sarif

      # =========================
      # 6) OPTIONAL - SonarQube
      # =========================
      - name: SonarQube Scan (optional)
        env:
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          if [ -z "${SONAR_HOST_URL}" ] || [ -z "${SONAR_TOKEN}" ]; then
            echo "SonarQube secrets not set -> skipping"
            exit 0
          fi

          mvn --no-transfer-progress -DskipTests sonar:sonar \
            -Dsonar.host.url="${SONAR_HOST_URL}" \
            -Dsonar.login="${SONAR_TOKEN}" || true

      # =========================
      # 7) Start WebGoat
      # =========================
      - name: Start WebGoat container
        run: |
          set -eux
          docker run -d --name webgoat \
            -p 8080:8080 -p 9090:9090 \
            $IMAGE:$TAG_SHA
          docker ps

      - name: Wait for WebGoat
        run: |
          set -eux
          for i in $(seq 1 90); do
            if curl -fsS http://127.0.0.1:8080/WebGoat/actuator/health >/dev/null; then
              echo "WebGoat is up!"
              exit 0
            fi
            sleep 5
          done
          echo "WebGoat not ready" >&2
          docker logs webgoat || true
          exit 1

      - name: Smoke test target
        run: |
          set -eux
          curl -I http://127.0.0.1:8080/WebGoat/ | head -n 20
          curl -fsS http://127.0.0.1:8080/WebGoat/actuator/health | cat

      # =========================
      # 7.1) DAST Pre-checks (IMPORTANT)
      # =========================
      - name: DAST network check from container namespace
        run: |
          set -eux
          docker run --rm --network container:webgoat-webgoat-1 curlimages/curl:8.5.0 \
            -sS -D- http://127.0.0.1:8080/WebGoat/ -o /dev/null | head -n 30

      - name: DAST crawl quick check (links)
        run: |
          set -eux
          html=$(curl -fsS http://127.0.0.1:8080/WebGoat/ | head -n 200 || true)
          echo "$html" | sed -n '1,60p'
          echo "$html" | grep -Eo 'href="[^"]+"' | head -n 30 || true

      # =========================
      # 8) DAST - ZAP baseline (spider + debug)
      # =========================
      - name: ZAP Baseline Scan (spider+debug)
        run: |
          set -eux
          mkdir -p reports/dast/zap

          docker run --rm --network container:webgoat-webgoat-1 \
            -v "$PWD/reports/dast/zap:/zap/wrk" \
            ghcr.io/zaproxy/zaproxy:stable \
            zap-baseline.py \
              -t http://127.0.0.1:8080/WebGoat/ \
              -m 3 \
              -s \
              -d \
              -r zap-report.html \
              -J zap-report.json \
              -x zap-report.xml \
              -I || true

          sudo chown -R $USER:$USER reports/dast/zap || true
          sudo chmod -R u+rwX reports/dast/zap || true
          echo "== ZAP files =="; ls -lah reports/dast/zap || true
          echo "== ZAP json head =="; head -n 80 reports/dast/zap/zap-report.json || true

          if [ ! -s reports/dast/zap/zap-report.json ]; then
            printf '%s\n' '{"meta":{"tool":"zap","note":"empty output (likely crawl/auth issue)"},"alerts":[]}' > reports/dast/zap/zap-report.json
          fi

      # =========================
      # 9) DAST - Nuclei (no interactsh + verbose)
      # =========================
      - name: Nuclei debug scan (no interactsh + verbose)
        run: |
          set -eux
          mkdir -p reports/dast/nuclei

          docker run --rm --network container:webgoat-webgoat-1 \
            -v "$PWD/reports/dast/nuclei:/out" \
            projectdiscovery/nuclei:latest \
            -u http://127.0.0.1:8080/WebGoat/ \
            -t http/misconfiguration/ \
            -t http/exposures/ \
            -severity low,medium,high,critical \
            -ni \
            -vv \
            -jsonl -o /out/nuclei.jsonl || true

          sudo chown -R $USER:$USER reports/dast/nuclei || true
          sudo chmod -R u+rwX reports/dast/nuclei || true
          echo "== Nuclei files =="; ls -lah reports/dast/nuclei || true
          echo "== Nuclei lines =="; wc -l reports/dast/nuclei/nuclei.jsonl || true
          echo "== Nuclei sample =="; head -n 20 reports/dast/nuclei/nuclei.jsonl || true

          if [ ! -s reports/dast/nuclei/nuclei.jsonl ]; then
            printf '%s\n' '{"meta":{"tool":"nuclei","note":"0 matches (likely templates not suitable for WebGoat)","target":"WebGoat"}}' > reports/dast/nuclei/nuclei.jsonl
          fi

      # =========================
      # 10) DAST - Wapiti (more depth + verbose)
      # =========================
      - name: Wapiti debug scan (depth+verbose)
        run: |
          set -eux
          mkdir -p reports/dast/wapiti

          docker run --rm --network container:webgoat-webgoat-1 \
            -v "$PWD/reports/dast/wapiti:/out" \
            cyberwatch/wapiti:latest \
            wapiti -u http://127.0.0.1:8080/WebGoat/ \
              -m sql,xss,file,xxe \
              -l 2 -d 5 \
              -v 2 \
              -f json -o /out/wapiti.json || true

          sudo chown -R $USER:$USER reports/dast/wapiti || true
          sudo chmod -R u+rwX reports/dast/wapiti || true
          echo "== Wapiti files =="; ls -lah reports/dast/wapiti || true
          echo "== Wapiti bytes =="; wc -c reports/dast/wapiti/wapiti.json || true
          echo "== Wapiti head =="; head -n 60 reports/dast/wapiti/wapiti.json || true

          if [ ! -s reports/dast/wapiti/wapiti.json ]; then
            printf '%s\n' '{"meta":{"tool":"wapiti","note":"empty output (likely crawl/auth/spa issue)"},"vulnerabilities":[]}' > reports/dast/wapiti/wapiti.json
          fi

      # =========================
      # 11) Debug summary
      # =========================
      - name: Debug DAST sizes (final)
        if: always()
        run: |
          echo "== ZAP bytes ==";    wc -c reports/dast/zap/zap-report.json || true
          echo "== Nuclei lines =="; wc -l reports/dast/nuclei/nuclei.jsonl || true
          echo "== Wapiti bytes =="; wc -c reports/dast/wapiti/wapiti.json || true

      - name: Stop WebGoat
        if: always()
        run: docker rm -f webgoat || true

      # =========================
      # 12) ETL (unified JSON)
      # =========================
      - name: Create ETL script (unify)
        run: |
          cat > ai/unify.py <<'PY'
          import json
          from pathlib import Path

          def load_json(path: Path, default):
            try:
              return json.loads(path.read_text(encoding="utf-8"))
            except Exception:
              return default

          def load_jsonl(path: Path):
            out=[]
            try:
              for line in path.read_text(encoding="utf-8").splitlines():
                line=line.strip()
                if not line:
                  continue
                try:
                  out.append(json.loads(line))
                except Exception:
                  pass
            except Exception:
              pass
            return out

          def safe_read(path: Path):
            try:
              return path.read_text(encoding="utf-8", errors="ignore")
            except Exception:
              return ""

          def main():
            base = Path("reports")
            unified = {
              "meta": {
                "repo": safe_read(Path(".git/config")),
                "generated_from": "github-actions",
              },
              "sast": {
                "semgrep": load_json(base/"sast/semgrep/semgrep.json", {}),
                "bearer":  load_json(base/"sast/bearer/bearer.json", {}),
              },
              "dast": {
                "zap":     load_json(base/"dast/zap/zap-report.json", {}),
                "nuclei":  load_jsonl(base/"dast/nuclei/nuclei.jsonl"),
                "wapiti":  load_json(base/"dast/wapiti/wapiti.json", {}),
              },
              "ai_summary": {
                "note": "placeholder (later: anomaly detection + NLP recommendations)"
              }
            }
            (base/"ai").mkdir(parents=True, exist_ok=True)
            (base/"ai/ai_unified_report.json").write_text(
              json.dumps(unified, indent=2, ensure_ascii=False),
              encoding="utf-8"
            )
            print("OK: reports/ai/ai_unified_report.json")

          if __name__ == "__main__":
            main()
          PY

      - name: Run ETL
        run: |
          python ai/unify.py
          echo "=== reports tree ==="
          ls -R reports || true

      # =========================
      # 13) Upload ONE artifact ZIP
      # =========================
      - name: Upload devsecops reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: devsecops-reports
          path: reports
          retention-days: 14
